language: go

go:
  - "1.13"
  - "tip"

go_import_path: github.com/aws/aws-xray-daemon

services:
  - docker

env:
  GO111MODULE: on

install:
  - go mod download

script:
  - make test

after_success:
  - make build-docker
  - make push-docker

matrix:
  allow_failures:
    - go: tip
