# The Go compiler is currently unable to produce read-only relocations
# (it produces static binaries).
hardening-no-relro /usr/bin/xray

# Amazon Xray Debian maintainer and upstream author are identical.
# Therefore see normal changelog file for Debian changes.
syntax-error-in-debian-changelog
